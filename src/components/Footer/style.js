import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    footer: {
        alignItems: 'center',
        zIndex: 1,
        marginTop: -100,
    },
    footerText: {
        fontSize: 20,
        marginTop: -15,
    },
});

export default styles;
